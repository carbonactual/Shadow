{
  "name": "shadow",
  "version": "0.1.0",
  "private": true,
  "description": "Shadow AI: user-bound minted AI agent platform for lawful content ownership, social media operations, affiliate workflows, dropshipping, surveys, remote work and monetization reporting.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace apps/api\" \"npm run dev --workspace apps/web\"",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start --workspace apps/api",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {}
}
