# Deployment

## Local

Run the API from `apps/api` and the web app from `apps/web`.

## API

```bash
cd apps/api
npm install
npm start
```

## Web

```bash
cd apps/web
npm install
npm run dev
```

## Environment

Copy the example environment files and fill the public app URL and API URL.
